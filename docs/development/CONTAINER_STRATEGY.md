# Container Strategy

Docker is not a GUI runtime requirement. Native Tauri packaging needs platform-specific host tooling. Containers may later support reproducible non-GUI checks, mock telemetry services, PX4 SITL, and CI jobs. No Dockerfile is included because it does not improve this local native prototype.
