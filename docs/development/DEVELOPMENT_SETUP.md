# Development Setup

Install Node, pnpm, Rust stable, and platform Tauri prerequisites. Run `pnpm install`, then `pnpm tauri dev`. Use `pnpm test`, `pnpm lint`, and `pnpm format:check` for frontend validation. Rust checks run with Cargo from `src-tauri`.
